services:
  - type: web
    name: insurance-ai-pipeline
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DEMO_MODE
        value: "true"
      - key: PORT
        value: "8000"
    plan: free
